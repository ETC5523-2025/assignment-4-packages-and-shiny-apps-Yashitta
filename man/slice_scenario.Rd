% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slice_scenario.R
\name{slice_scenario}
\alias{slice_scenario}
\title{Slice the scenario grid (nearest R0 / VE / coverage)}
\usage{
slice_scenario(data, r0, ve, coverage)
}
\arguments{
\item{data}{A tibble/data.frame containing columns \code{r0}, \code{ve}, \code{coverage}
(typically \link{ob_summary} or one of its filtered variants).}

\item{r0}{Numeric, desired basic reproduction number.}

\item{ve}{Numeric in [0, 1], desired vaccine effectiveness.}

\item{coverage}{Numeric in [0, 1], desired vaccine coverage.}
}
\value{
A 1-row tibble with the matched scenario, including:
\describe{
\item{r0}{Nearest R0 on the grid.}
\item{ve}{Nearest VE on the grid.}
\item{coverage}{Nearest coverage on the grid.}
\item{traveller_ob_prob}{Outbreak probability (traveller pathway).}
\item{worker_ob_prob}{Outbreak probability (worker pathway).}
\item{chance50}{Median days to outbreak.}
\item{chance95}{95th-percentile days to outbreak.}
}
}
\description{
Convenience helper for \code{quarrisk} datasets. Given a desired basic
reproduction number (R0), vaccine effectiveness (VE), and vaccine coverage,
this finds the \strong{nearest} scenario on the model grid (e.g. \link{ob_summary})
and returns that row as a tibble.
}
\details{
“Nearest” is computed independently along each axis (R0, VE, coverage) by
choosing the grid value with minimum absolute difference to the requested
value. This mirrors how sliders in the Shiny app snap to the nearest
available scenario.
}
\examples{
\dontrun{
  data(ob_summary)
  slice_scenario(ob_summary, r0 = 2, ve = 0.80, coverage = 0.80)
}
}
\seealso{
\link{ob_summary}, \link{ob_probs_travellers}, \link{ob_timings_summary}
}
